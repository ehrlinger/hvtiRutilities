% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_layout.R
\name{study_dir}
\alias{study_dir}
\title{Resolve a directory in the study layout}
\usage{
study_dir(folder, root = study_root())
}
\arguments{
\item{folder}{Character. One logical study directory name.}

\item{root}{Character. Study root. Defaults to \code{study_root()}.}
}
\value{
Character(1). The resolved directory path. Its existence is not
  required.
}
\description{
Maps a logical study directory such as \code{"datasets"} to the numbered
spelling used by new studies or the bare spelling retained by legacy
studies. A root containing both layouts is an error.
}
\examples{
root <- file.path(tempdir(), "numbered-study-layout")
dir.create(file.path(root, "00_datasets"), recursive = TRUE,
           showWarnings = FALSE)
study_dir("datasets", root)
unlink(root, recursive = TRUE)
}
\seealso{
\code{\link{study_root}}, \code{\link{sas_path}}
}
