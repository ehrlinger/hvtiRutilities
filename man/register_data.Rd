% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register_data.R
\name{register_data}
\alias{register_data}
\title{Register a study dataset}
\usage{
register_data(
  root = getwd(),
  built,
  dataset = "study",
  role = c("study", "named"),
  population = NULL,
  source = NULL,
  extract_date = NULL,
  catalog_dataset = NULL,
  release_id = NULL
)
}
\arguments{
\item{root}{Character. Study root or a directory beneath it.}

\item{built}{Character(1). Dataset filename within the logical
\code{datasets} directory, including its extension.}

\item{dataset}{Character(1). Logical dataset name. \code{"study"} is
reserved for the default.}

\item{role}{Character. Either \code{"study"} or \code{"named"}.}

\item{population}{Character(1) or \code{NULL}. Population description.}

\item{source}{Character(1) or \code{NULL}. Data-source description.}

\item{extract_date}{Character, \code{Date}, or \code{NULL}. Extraction
date. The file modification date is used when omitted.}

\item{catalog_dataset, release_id}{Character(1) or \code{NULL}. Producer
catalog dataset ID and exact published release ID. Supply both to make
the study contract release-aware, or neither for a legacy registration.}
}
\value{
An object of class \code{"study_status"}, returned visibly.
}
\description{
Completes the default study data contract or adds one distinctly named
dataset. The function derives row and column counts from the file and
replaces \code{_study.yml} and \code{manifest.yaml} only after both updated
files have been prepared successfully.
}
\seealso{
\code{\link{study_setup}}, \code{\link{study_config}},
  \code{\link{update_manifest}}
}
