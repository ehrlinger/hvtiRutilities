% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{capture_provenance}
\alias{capture_provenance}
\title{Capture an explicit job provenance payload}
\usage{
capture_provenance(
  job,
  data,
  artifacts = list(),
  extra = list(),
  cfg = study_config()
)
}
\arguments{
\item{job}{Character(1). Stable job identity.}

\item{data}{List of records returned by \code{\link{provenance_data}}.}

\item{artifacts}{List of records returned by
\code{\link{provenance_artifact}}.}

\item{extra}{Named list of job-specific fields. Reserved capture and
publication fields cannot be displaced.}

\item{cfg}{List. A study manifest from \code{\link{study_config}}.}
}
\value{
A captured provenance payload as a plain list.
}
\description{
Freezes the study identity, executing R session, loaded packages, lockfile,
and explicit data and artifact records. Capture never resolves a current
dataset implicitly. Pass \code{list()} deliberately when a job has no known
direct data input.

Capture does not write a sidecar or inspect a rendered output. Use
\code{\link{publish_provenance}} only after the completed output exists.
}
\seealso{
\code{\link{publish_provenance}},
  \code{\link{record_provenance}}
}
