% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_checklist.R
\name{study_checklist}
\alias{study_checklist}
\title{Render a study audit as a markdown checklist}
\usage{
study_checklist(status, path = NULL)
}
\arguments{
\item{status}{An object of class \code{"study_status"}, from
\code{\link{study_status}} or \code{\link{study_init}}.}

\item{path}{Character(1) or \code{NULL}. Where to write the markdown. When
\code{NULL} (default) the lines are returned instead of written.}
}
\value{
When \code{path} is \code{NULL}, a character vector of markdown
  lines. Otherwise \code{path}, invisibly, after writing.
}
\description{
Turns a \code{\link{study_status}} result into markdown: one checkbox per
check, ticked where the check passed and left open otherwise, with the
detail alongside. File counts follow.
}
\examples{
root <- file.path(tempdir(), "study-checklist-example")
dir.create(root, showWarnings = FALSE)
cat(study_checklist(study_status(root)), sep = "\n")
unlink(root, recursive = TRUE)
}
\seealso{
\code{\link{study_status}}, \code{\link{study_init}}
}
