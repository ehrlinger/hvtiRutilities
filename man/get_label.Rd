% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_map.R
\name{get_label}
\alias{get_label}
\title{Look up the label for a single variable}
\usage{
get_label(label_map_df, variable)
}
\arguments{
\item{label_map_df}{A data frame with \code{key} and \code{label} columns,
as returned by \code{\link{label_map}}.}

\item{variable}{A single character string: the variable name to look up.}
}
\value{
A single character string: the label for the requested variable.
}
\description{
Returns the descriptive label for one variable name from a label map.
This is a safer alternative to the manual \code{match()} pattern,
providing clear errors on typos and missing variables.
}
\examples{
dta <- generate_survival_data(n = 50, seed = 42)
lmap <- label_map(dta)

get_label(lmap, "age")
get_label(lmap, "hgb_bs")

# Use in plot titles
var <- "lvefvs_b"
plot(dta[[var]], main = get_label(lmap, var), ylab = get_label(lmap, var))
}
